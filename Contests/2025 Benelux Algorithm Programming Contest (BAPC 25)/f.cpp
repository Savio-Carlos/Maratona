#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define winton ios_base::sync_with_stdio(false),cin.tie(NULL),cout.tie(NULL)
signed main(){
    winton;
    map<int,set<int>> message;
 
    string s;
    cin >> s;
    if (s == "send"){
        int x;
        cin >> x;
        
    else {
        int a, b;
        cin >> a >> b;
        
    }
}
